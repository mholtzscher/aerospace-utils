# Invalid config file returns error.

! exec aerospace-utils gaps use --config-path config.toml --state-path state.toml --monitor-width 1920 --no-color 50
stderr 'update config'

-- config.toml --
[gaps
this is not valid toml syntax

-- state.toml --
[monitors.main]
current = 50
default = 50

[monitors.'Built-in Retina Display']
current = 75
default = 60
