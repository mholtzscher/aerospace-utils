# Unknown monitor without state returns error.

cp $AEROSPACE_TESTDATA/aerospace.toml config.toml
cp $AEROSPACE_TESTDATA/state.toml state.toml

! exec aerospace-utils gaps adjust --monitor 'NonExistent Monitor' --config-path config.toml --state-path state.toml --monitor-width 1920 --no-color
stderr 'no current percentage'
