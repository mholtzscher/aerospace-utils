# Unknown monitor name still works (creates new entry).

cp $AEROSPACE_TESTDATA/aerospace.toml config.toml
cp $AEROSPACE_TESTDATA/state.toml state.toml

exec aerospace-utils gaps use --dry-run --monitor 'NonExistent Monitor' --config-path config.toml --state-path state.toml --monitor-width 1920 --no-color 50
stdout 'NonExistent Monitor'
