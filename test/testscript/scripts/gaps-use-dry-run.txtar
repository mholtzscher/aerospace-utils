# Dry run does not modify files.

cp $AEROSPACE_TESTDATA/aerospace.toml config.toml
cp $AEROSPACE_TESTDATA/state.toml state.toml
cp $AEROSPACE_TESTDATA/aerospace.toml config-expected.toml

exec aerospace-utils gaps use --dry-run --config-path config.toml --state-path state.toml --monitor-width 1920 --no-color 50
stdout 'dry'
stdout '50%'
stdout '480px'

cmp config.toml config-expected.toml
