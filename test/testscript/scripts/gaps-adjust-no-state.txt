# No state file returns error.

cp $AEROSPACE_TESTDATA/aerospace.toml config.toml

! exec aerospace-utils gaps adjust --config-path config.toml --state-path nonexistent.toml --monitor-width 1920 --no-color
stderr 'no current percentage'
