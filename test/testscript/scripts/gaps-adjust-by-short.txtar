# Short -b flag works.

cp $AEROSPACE_TESTDATA/aerospace.toml config.toml
cp $AEROSPACE_TESTDATA/state.toml state.toml

exec aerospace-utils gaps adjust --dry-run --config-path config.toml --state-path state.toml --monitor-width 1920 --no-color -b 5
stdout '55%'
