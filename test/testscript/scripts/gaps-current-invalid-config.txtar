# Invalid config file shows error.

exec aerospace-utils workspace current --config-path config.toml --state-path nonexistent.toml --no-color
stdout 'Error loading config'
stdout '\(file not found\)'

-- config.toml --
[gaps
this is not valid toml syntax
