# Missing state file is created.

cp $AEROSPACE_TESTDATA/aerospace.toml config.toml

exec aerospace-utils gaps use --config-path config.toml --state-path state.toml --monitor-width 1920 --no-color 55
stdout 'Set main to 55%'
grep 'current = 55' state.toml
grep 'default = 55' state.toml
